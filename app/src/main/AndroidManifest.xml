<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.senai.sp.jandira.calculadora">

    <application
        android:allowBackup="true"
        android:icon="@drawable/heal"
        android:label="@string/app_name"
        android:roundIcon="@drawable/heal"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".gui.NCDActivity"></activity>
        <activity android:name=".gui.splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".gui.MenuActivity" />
        <activity android:name=".gui.ImcActivity" />
        <activity android:name=".gui.resultado" />
    </application>

</manifest>